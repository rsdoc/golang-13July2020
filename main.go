// collections of go files which belongs to same package ~ project ~ workspace
// all files belonging to main package - must declare package name at the top of the files
package main

import "fmt"

func main() {
	fmt.Println("Hello world from Golang ✍")
}
